from setuptools import find_packages, setup

setup(
    name='cognitron_vl',
    version='0.0.1',
    packages=[
        "cognitron_vl",
        "lcvlm_modellink",
    ],
    install_requires=[
    ],
)

